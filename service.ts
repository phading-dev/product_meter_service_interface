import { ServiceDescriptor } from '@selfage/service_descriptor';

export let METER_NODE_SERVICE: ServiceDescriptor = {
  name: "MeterNodeService",
  path: "/m/n",
}

export let METER_WEB_SERVICE: ServiceDescriptor = {
  name: "MeterWebService",
  path: "/m/w",
}
